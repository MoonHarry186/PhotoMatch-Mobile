import { TabPlaceholderScreen } from '@/features/navigation/tab-placeholder-screen';

export default function NearbyRoute() {
  return (
    <TabPlaceholderScreen
      title="Gần đây"
      description="Bản đồ chỉ hiển thị tọa độ công khai gần đúng từ backend."
    />
  );
}
